<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state -->
    <item android:color="@color/brand_green" android:state_checked="true"/>
    <!-- Default (unchecked) -->
    <item android:color="@color/ss_hint"/>
</selector>
