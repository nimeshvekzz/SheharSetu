<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

<!-- Base app theme (Material 3, no ActionBar) -->
<style name="Base.Theme.SheharSetu" parent="Theme.MaterialComponents.Light.NoActionBar">
    <!-- System bars from the very first frame -->
    <item name="android:statusBarColor">@android:color/black</item>
    <item name="android:navigationBarColor">@android:color/black</item>

    <!-- White icons on the black status bar -->
    <item name="android:windowLightStatusBar">false</item>
</style>

<!-- App theme alias -->
<style name="Theme.SheharSetu" parent="Base.Theme.SheharSetu" />

    <style name="Theme.SheharSetu.M3" parent="Theme.Material3.Light.NoActionBar">
        <!-- Reuse your brand colors so UI matches the rest of the app -->
        <item name="colorPrimary">@color/brand_green</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/black</item>

        <!-- Defaults so you don't have to set style= on every widget -->
        <item name="textInputStyle">@style/Widget.Material3.TextInputLayout.OutlinedBox</item>
        <item name="materialButtonStyle">@style/Widget.Material3.Button</item>
        <item name="chipStyle">@style/Widget.Material3.Chip.Suggestion</item>
    </style>
</resources>
