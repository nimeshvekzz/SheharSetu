<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    app:statusBarBackground="@android:color/black"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- ========= Your existing screen kept exactly (inside a container) ========= -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

        <!-- ===== HEADER (unchanged) ===== -->
        <ImageView
            android:id="@+id/topImage"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="fitXY"
            android:src="@drawable/hedar2"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_default="percent"
            app:layout_constraintHeight_percent="0.10" />

        <!-- Controls overlay (same bounds as header) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/headerOverlay"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            app:layout_constraintTop_toTopOf="@id/topImage"
            app:layout_constraintBottom_toBottomOf="@id/topImage"
            app:layout_constraintStart_toStartOf="@id/topImage"
            app:layout_constraintEnd_toEndOf="@id/topImage">

            <!-- Drawer (vertically centered) -->
            <ImageView
                android:id="@+id/btnDrawer"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:padding="6dp"
                android:contentDescription="Menu"
                android:src="@drawable/ic_menu_24"
                app:tint="@android:color/white"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" />

            <!-- Search bar (50dp tall, centered vertically) -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/tiSearch"
                style="@style/Widget.MaterialComponents.TextInputLayout.FilledBox.Dense"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_search_translucent"
                android:elevation="1dp"
                android:paddingTop="0dp"
                android:paddingBottom="0dp"
                app:boxBackgroundMode="none"
                app:startIconDrawable="@drawable/ic_search_24"
                app:startIconTint="@android:color/black"
                app:startIconMinSize="24dp"
                app:endIconMode="custom"
                app:endIconDrawable="@drawable/ic_mic_24"
                app:endIconTint="@android:color/white"
                app:endIconMinSize="24dp"
                app:layout_constraintStart_toEndOf="@id/btnDrawer"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/etSearch"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:hint="Search products, cities…"
                    android:imeOptions="actionSearch"
                    android:inputType="text"
                    android:singleLine="true"
                    android:textSize="16sp"
                    android:textColor="@color/black"
                    android:textColorHint="#CCFFFFFF"
                    android:gravity="center_vertical|start"
                    android:includeFontPadding="false"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="0dp"
                    android:paddingBottom="0dp"/>
            </com.google.android.material.textfield.TextInputLayout>


        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Horizontal categories -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvCategories"
            android:layout_width="0dp"
            android:layout_height="84dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:clipToPadding="false"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toBottomOf="@id/topImage"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/item_category"/>

        <!-- Section title -->
        <TextView
            android:id="@+id/tvSectionTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="10dp"
            android:text="@string/featured_listings"
            android:textStyle="bold"
            android:textSize="20sp"
            android:textColor="@android:color/black"
            app:layout_constraintTop_toBottomOf="@id/rvCategories"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Subfilters -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvSubFiltersGrid"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="12dp"
            android:clipToPadding="false"
            android:visibility="gone"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="3"
            app:layout_constraintTop_toBottomOf="@id/tvSectionTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/item_subfilter_grid"/>

        <!-- New/Used toggle -->
        <com.google.android.material.button.MaterialButtonToggleGroup
            android:id="@+id/toggleNewOld"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="10dp"
            android:padding="3dp"
            android:visibility="gone"
            app:singleSelection="true"
            app:selectionRequired="false"
            app:layout_constraintTop_toBottomOf="@id/rvSubFiltersGrid"
            app:layout_constraintStart_toStartOf="parent">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnShowNew"
                style="@style/SheharToggleButton"
                android:layout_width="92dp"
                android:layout_height="match_parent"
                android:text="@string/toggle_new"
                android:textAllCaps="false"
                app:shapeAppearanceOverlay="@style/ShapeSquareLeft" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnShowOld"
                style="@style/SheharToggleButton"
                android:layout_width="92dp"
                android:layout_height="match_parent"
                android:text="@string/toggle_old"
                android:textAllCaps="false"
                app:shapeAppearanceOverlay="@style/ShapeSquareRight" />
        </com.google.android.material.button.MaterialButtonToggleGroup>

        <!-- Products -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvProducts"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="12dp"
            android:clipToPadding="false"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="2"
            app:layout_constraintTop_toBottomOf="@id/toggleNewOld"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/item_product"/>

        <!-- Floating GLASS banner -->
        <include
            android:id="@+id/helpBanner"
            layout="@layout/view_help_banner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="6dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ========= Drawer menu ========= -->
    <!--    <com.google.android.material.navigation.NavigationView-->
    <!--        android:id="@+id/navView"-->
    <!--        android:layout_width="280dp"-->
    <!--        android:layout_height="match_parent"-->
    <!--        android:layout_gravity="start"-->
    <!--        android:fitsSystemWindows="true"-->
    <!--        android:background="@color/drawerSurface"-->
    <!--        app:headerLayout="@layout/av_header_main"-->
    <!--        app:menu="@menu/nav_main"-->
    <!--        app:itemIconTint="@null"-->
    <!--        app:itemTextColor="@color/white"-->
    <!--        app:itemRippleColor="@color/nav_item_ripple"/>-->

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/av_header_main"
        app:menu="@menu/nav_main"
        app:itemIconTint="@null"
        app:itemTextColor="@color/white" />


</androidx.drawerlayout.widget.DrawerLayout>
